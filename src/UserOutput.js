import React from 'react';

const useroutput=(props)=>
{
    return(
        <div>
            <p>Iam {props.username}</p>
            <p>Iam {props.username}</p>
        </div>
    );
}
export default useroutput;